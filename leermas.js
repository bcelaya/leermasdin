// esto es para generar dinámicamente los leer mas de las categorias
function leermas(){
jQuery(document).ready(function($) {
    // $() will work as an alias for jQuery() inside of this function
    $('#leermas').slideToggle();
});
}